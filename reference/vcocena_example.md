# Example data paths for vcocena

Returns file paths for the small example dataset included with the
package. Use these paths to build `layers` and `supplement` inputs for
\coderun_vcocena().

## Usage

``` r
vcocena_example()
```

## Value

A named list with paths to example count tables, annotations, and
reference files.
